#include <stdio.h>

int main(void) {
    int t,i;
    int x,y;
    scanf("%d",&t);
    
    for(i=0;i<t;i++)
    {
        scanf("%d %d",&x,&y);
        
        if(x>y)
        {
            printf("SECOND\n");
        }
        else if(x<y)
        {
            printf("FIRST\n");
        }
        else if(x=y)
        {
            printf(" ANY\n");
        }
    }
	// your code goes here
	return 0;
}

